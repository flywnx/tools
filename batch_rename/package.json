{
  "name": "batch_rename",
  "version": "0.0.1",
  "description": "批量文件改名",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ --all --out ../../batch_rename --overwrite --icon=./batch_rename.ico"
  },
  "author": "jlb",
  "license": "ISC",
  "devDependencies": {
    "electron": "^7.2.4",
    "fs": "^0.0.1-security"
  }
}
